#include <stdio.h>
/**
 *main - prints a random number as positive, zero, or negative depending
 *on relation to zero
 *Return: returns 0
 */

int main (void)
{
	char a;
	for(a = 'a'; a <= 'z'; a++) {
		putchar(a);
		putchar('\n');
			}
	return 0;
}
